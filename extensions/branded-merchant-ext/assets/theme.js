// theme.js (or theme.js.liquid)

document.addEventListener('DOMContentLoaded', function () {
  var metafieldElement = document.querySelector('.metafield-element');
  var metafieldValue = metafieldElement.innerHTML.trim();

  // Use the metafieldValue as needed
});
